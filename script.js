// Exemple de fonction pour un bouton de jeu
document.querySelectorAll("button").forEach(button => {
    button.addEventListener("click", () => {
        console.log("Jeu lancé depuis MoonZone!");
    });
});
